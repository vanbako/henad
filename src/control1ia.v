// control1ia.v
module control1ia();
    // Stage 1 Instruction Address logic and latch
    // The IA stage currently has no signals defined, so the
    // stage and latch are instantiated without ports.

    stage1ia  u_stage1ia();
    latch1iaif u_latch1iaif();
endmodule
